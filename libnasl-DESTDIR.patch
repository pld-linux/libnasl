diff -urN libnasl.orig/Makefile libnasl/Makefile
--- libnasl.orig/Makefile	Tue Oct 16 18:54:04 2001
+++ libnasl/Makefile	Fri Sep 27 19:19:39 2002
@@ -9,13 +9,15 @@
 
 install ::
 	cd nasl && ${MAKE} install
-	test -d ${includedir}/nessus || $(INSTALL_DIR) -m 755 ${includedir}/nessus
-	$(INSTALL) -c -m 0444 include/nasl.h ${includedir}/nessus
-	test -d ${bindir} || $(INSTALL_DIR) -m 755 ${bindir}
-	$(INSTALL) -c -m 0755 nasl-config ${bindir}/nasl-config
-	test -d ${mandir}/man1 || $(INSTALL_DIR) -m 755 ${mandir}/man1
-	$(INSTALL) -c -m 0644 doc/nasl-config.1 ${mandir}/man1
-	$(INSTALL) -c -m 0644 doc/nasl.1 ${mandir}/man1
+	test -d $(DESTDIR)${includedir}/nessus || \
+		$(INSTALL_DIR) -m 755 $(DESTDIR)${includedir}/nessus
+	$(INSTALL) -c -m 0444 include/nasl.h $(DESTDIR)${includedir}/nessus
+	test -d $(DESTDIR)${bindir} || $(INSTALL_DIR) -m 755 $(DESTDIR)${bindir}
+	$(INSTALL) -c -m 0755 nasl-config $(DESTDIR)${bindir}/nasl-config
+	test -d $(DESTDIR)${mandir}/man1 || \
+		$(INSTALL_DIR) -m 755 $(DESTDIR)${mandir}/man1
+	$(INSTALL) -c -m 0644 doc/nasl-config.1 $(DESTDIR)${mandir}/man1
+	$(INSTALL) -c -m 0644 doc/nasl.1 $(DESTDIR)${mandir}/man1
 
 
 	@echo
diff -urN libnasl.orig/nasl/Makefile libnasl/nasl/Makefile
--- libnasl.orig/nasl/Makefile	Mon Oct 15 19:58:07 2001
+++ libnasl/nasl/Makefile	Fri Sep 27 19:30:18 2002
@@ -40,10 +40,13 @@
 	-version-info ${PACKAGE_VERSION}
 
 install : libnasl.la nasl
-	test -d ${libdir} || $(INSTALL_DIR) -m 755 ${libdir}
-	$(LIBTOOL) --mode install $(INSTALL) libnasl.la ${libdir}/libnasl.la
-	$(LIBTOOL) --finish ${libdir}
-	$(INSTALL) nasl ${bindir}/
+	test -d $(DESTDIR)${libdir} || $(INSTALL_DIR) -m 755 $(DESTDIR)${libdir}
+	$(LIBTOOL) --mode install $(INSTALL) libnasl.la \
+		$(DESTDIR)${libdir}/libnasl.la
+	$(LIBTOOL) --finish $(DESTDIR)${libdir}
+	test -d $(DESTDIR)${bindir} || $(INSTALL_DIR) -m 755 $(DESTDIR)${bindir}
+	$(INSTALL) nasl $(DESTDIR)${bindir}
+
 nessus_extensions.o : cflags nessus_extensions.c
 	$(COMPILE) -c nessus_extensions.c 
 
